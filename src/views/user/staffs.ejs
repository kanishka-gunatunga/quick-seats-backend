<%- include('../layouts/header.ejs') %>
<!-- ============================================================== -->
            <!-- Start Page Content here -->
            <!-- ============================================================== -->
         
            <div class="content-page">
                <div class="content">

                    <!-- Start Content-->
                    <div class="container-fluid">

                        <div class="row">
                            <div class="col-12">
                                <div class="card">
                                    <div class="card-body">
                                        <h4 class="header-title">Staff Members</h4>
                                                <% if (success) { %>
                                                    <div class="alert alert-success"><%= success %></div>
                                                <% } %>
                                                <% if (error) { %>
                                                    <div class="alert alert-danger"><%= error %></div>
                                                <% } %>

                                         <div class="row">
                                            <div class="col-12">
                                                <div class="card">
                                                    <div class="card-body table-responsive">
                                                       
                    
                                                        <table id="responsive-datatable" class="table table-bordered table-bordered dt-responsive nowrap">
                                                            <thead>
                                                            <tr>
                                                                <th>Name</th>
                                                                <th>Phone No</th>
                                                                <th>Email</th>
                                                                <th>Status</th>
                                                                <th>Actions</th>

                                                            </tr>
                                                            </thead>
                    
                    
                                                           <tbody>
                                                                <% staffs.forEach(staff => { %>
                                                                    <tr>
                                                                    <td><%= staff.staffDetails?.name || 'N/A' %></td>
                                                                    <td><%= staff.staffDetails?.phone || 'N/A' %></td>
                                                                    <td><%= staff.email %></td>
                                                                    <td>
                                                                        <span class="badge <%= staff.status === 'active' ? 'bg-success' : 'bg-danger' %>">
                                                                        <%= staff.status %>
                                                                        </span>
                                                                    </td>
                                                                    <td>
                                                                        <a href="/staff/edit/<%= staff.id %>" class="btn btn-primary rounded-pill waves-effect waves-light">Edit</a>
                                                                        <% if (staff.status === 'active') { %>
                                                                        <a href="/staff/deactivate/<%= staff.id %>" class="btn btn-danger rounded-pill waves-effect waves-light">Deactivate</a>
                                                                        <% } else { %>
                                                                        <a href="/staff/activate/<%= staff.id %>" class="btn btn-success rounded-pill waves-effect waves-light">Activate</a>
                                                                        <% } %>
                                                                    </td>
                                                                   
                                                                    </tr>
                                                                <% }); %>
                                                            </tbody>
                                                        </table>
                                                    </div>
                                                </div>
                                            
                                            </div>
                                        </div> <!-- end row -->   

                                    </div> <!-- end card-body -->
                                </div> <!-- end card -->
                            </div>
                        </div>
                        <!-- end row -->        
                        
                    
                    </div> <!-- container -->

                </div>

            </div>
            <!-- ============================================================== -->
            <!-- End Page content -->
            <!-- ============================================================== -->
<%- include('../layouts/footer.ejs') %>